{
  "name": "flight-schedules",
  "version": "2.2.0-beta",
  "description": "Easy way to populate your virtual airlines with informations!",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/virtualegret/flight-schedules.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "airport-info": "^1.0.4",
    "axios": "^0.21.4",
    "electron-is-dev": "^2.0.0",
    "electron-prompt": "^1.7.0",
    "electron-updater": "^4.0.6",
    "encoding": "^0.1.13"
  }
}

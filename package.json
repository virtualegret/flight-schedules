{
  "name": "flight-schedules",
  "version": "2.2.3-beta",
  "description": "Easy way to populate your virtual airlines with information!",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/virtualegret/flight-schedules.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "virtualEgret",
  "license": "MIT",
  "devDependencies": {
    "electron": "^23.0.0",
    "electron-builder": "^23.6.0"
  },
  "dependencies": {
    "@electron/remote": "^2.0.9",
    "airport-info": "^1.0.4",
    "axios": "^0.21.4",
    "electron-is-dev": "^2.0.0",
    "electron-prompt": "^1.7.0",
    "electron-updater": "^4.0.6",
    "encoding": "^0.1.13",
    "jquery": "^3.6.3"
  }
}
